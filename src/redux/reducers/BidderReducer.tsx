import { } from '../actions/types';

const initState = {
    liquiditys: [],
    refresh: 0
};

export default function todo(state = initState, action: any) {
    switch (action.type) {

        default:
            return state
    }
}